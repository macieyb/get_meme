{% block body %}
    {% for comment in comments %}
        <div class="card">
            <div class="card-header text-muted">
                <small>
                    {{ comment.addedAt|date("M j, Y") }} <a href="{{ path('user_by_id', {"id":comment.user.id}) }}">{{ comment.user.username }}</a>
                </small>
            </div>
            <div class="card-body card-text">
                <p>{{ comment.content }}</p>
            </div>
        </div></br>
    {% endfor %}
{% endblock %}
